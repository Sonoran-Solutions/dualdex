*.iml
.gradle
/local.properties
/.idea/
.DS_Store
build/
app/build/
.externalNativeBuild
.cxx
test_runner
calc_test_runner
*.o
*.apk
*.log
cores/build-*
cores/mgba-src/
tools/calc-bundler/node_modules/

# Release signing secrets — never commit production keys or credentials.
*.jks
*.keystore
keystore.properties
signing.properties

# Developer-only runtime probe binaries (source is committed, the artifact is not).
tools/hns-runtime-probe/runtime_battle_probe

# Python bytecode from the developer-only map-data generator.
__pycache__/
*.pyc
